fn main() {
      let a : i32 = 55;
      let b : i32 = -22;

      1 < 2 || 2 > -3 || a <= b || a >= (-b + 2) || (1 + 2) == (3 * 4);
      1 + 2 - (3 * 4) / 6 % 7;
      -1 + -2 - (-3 * -4) / -6 % -7;
      (-a + -b - -a) * -b / -a % -b;
      1 == 2 || a != b;
}
